apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-config
  namespace: arogya
data:
  NODE_ENV: "production"
  PORT: "8081"
  JWT_EXPIRES_IN: "15m"
  JWT_REFRESH_EXPIRES_IN: "7d"
  BCRYPT_ROUNDS: "12"
