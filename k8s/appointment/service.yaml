apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: arogya
  labels:
    app: appointment-service
spec:
  type: ClusterIP
  selector:
    app: appointment-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
